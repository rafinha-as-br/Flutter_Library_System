import 'package:flutter/material.dart';

class BookLoansTab extends StatelessWidget {
  const BookLoansTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('empréstimos'),);
  }
}
