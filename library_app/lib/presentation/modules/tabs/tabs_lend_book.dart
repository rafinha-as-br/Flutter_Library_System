import 'package:flutter/material.dart';

class LendBookTab extends StatelessWidget {
  const LendBookTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Emprestar livro'),);
  }
}
