import 'package:flutter/material.dart';

class SearchPersonTab extends StatelessWidget {
  const SearchPersonTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('teste'),);
  }
}
